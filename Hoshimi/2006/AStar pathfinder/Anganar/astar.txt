Привет.

Посылаю результат моих экспериментов с A*.

Версия pathfinder`а вполне рабочая. В том смысле, что я не нашёл ни програмных, ни логических ошибок.

Учитывает типы местности и течения. Лучший путь находит гарантировано. Но медленно.  :(  Заметно медленнее дефолтного. В принципе, так и должно быть. Если снять требование "лучшего пути" и свести его до уровня дефолтного pathfinder`а, то эта радость ускоряется раз в 5-6... Так что над скоростью ещё колдую.

Используется примерно так:

1). Объявление
class MyAI : VG.Common.Player 
{
		public AStarPathfinder Pathfinder;

2). Создание
void MyAI_ChooseInjectionPointEvent()
{
		Pathfinder = new AStarPathfinder(this);

3). Исползование
	MoveTo(_player.Pathfinder.FindPath(this.Location,this.TargetPoint));

Если что - пиши.

Пока.